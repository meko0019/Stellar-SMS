language: python

services:
  - docker

before_install:
  - docker --version
  - docker-compose build web 
  - docker-compose up -d
  - docker ps -a

script:
  - docker-compose run --rm web sh
  - pytest